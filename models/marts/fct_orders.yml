version: 2

models:
  - name: fct_orders
    description: '{{ doc("orders_docs") }}'
    tests:
      - unique:
          column_name: "(order_id || '-' || product_id)"
    columns:
      - name: order_id
        description: Unique ID for this table
          - not_null